tls:
  certificates:
    - certFile: cert.pem
      keyFile: privkey.pem
      stores:
        - default
  stores:
    default:
      defaultCertificate:
        certFile: cert.pem
        keyFile: privkey.pem
  options:
    default:
      sniStrict: true