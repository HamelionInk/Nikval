tls:
  certificates:
    - certFile: /config/cert.pem
      keyFile: /config/privkey.pem
      stores:
        - default
  stores:
    default:
      defaultCertificate:
        certFile: /config/cert.pem
        keyFile: /config/privkey.pem
  options:
    default:
      sniStrict: true